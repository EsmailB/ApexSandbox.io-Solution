public Map<String, Account> phoneToAccount(List<Account> accounts) {
    //code here

        Map <String, Account> result = new Map<String, Account>();
        
        for(Contact a: accounts)
            if (a.Phone != null)
                result.put(a.phone, a.id);

        return result;
}