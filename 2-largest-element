public static Integer findLargest(List<Integer> nums) {
	//code here
    
    Integer maxv = nums[0];

    for(Integer i = 1; i < nums.size(); i++)
        if(nums[i] > maxv)
            maxv = nums[i];

    return maxv;
}