public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here

    return (c.AccountId != null || opp.AccountId != null) 
            && c.AccountId == opp.AccountId && c.AccountId == acc.Id;
    }
}