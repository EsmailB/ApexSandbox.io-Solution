public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here

    return (con.AccountId != null || opp.AccountId != null) 
            && con.AccountId == opp.AccountId && c.AccountId == acc.Id;
    }
}